require 'spec_helper'

describe SpreeFilterGrid do
  it 'has a version number' do
    expect(SpreeFilterGrid::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
